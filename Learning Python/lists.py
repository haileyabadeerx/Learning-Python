albums = ["Lover", "Reputation", "1989"]

#pwede rin mixed var types in one like
#albums = ["Red", 5, True] (string, int, and bool in one list)

print(albums[0])

#to access index from the last element
print(albums[-1])